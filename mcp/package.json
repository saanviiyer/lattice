{
  "name": "lattice-mcp",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Read-only MCP server exposing a lattice workspace to Claude and other MCP clients.",
  "bin": {
    "lattice-mcp": "./dist/lattice-mcp.mjs"
  },
  "scripts": {
    "build": "node build.mjs",
    "test": "node --test test/*.test.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0"
  },
  "devDependencies": {
    "esbuild": "^0.25.12"
  }
}
